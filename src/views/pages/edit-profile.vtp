<!-- Header -->
<div class="mb-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb mb-2">
            <li class="breadcrumb-item">
                <a href="/" router-link>Dashboard</a>
            </li>
            @component('breadcrumb', {
                breadcrumbs: [
                    {label: 'Profile', route: '/profile'}
                ],
                title: 'Edit Profile'
            })
        </ol>
    </nav>

    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
        <div>
            <h2 class="fw-bold mb-1">
                Edit Profile
            </h2>
            <p class="text-muted mb-0">
                Change the value of the field to edit profile
            </p>
        </div>
    </div>
</div>

<!-- Table Card -->
<div class="card shadow-sm mb-4">
    <form id="update-profile-form" method="post" class="m-0">
        <div class="card-body p-3 mt-3">
            @for(const field of fields)
            @component('vtp-input', {
                field, data: userData
            })
            @endfor
        </div>
        <div class="card-footer p-3">
            <a href="/profile" class="btn btn-normal btn-secondary" router-link>Cancel</a>
            <button class="btn btn-normal btn-primary btn-submit">Submit</button>
        </div>
    </form>
</div>